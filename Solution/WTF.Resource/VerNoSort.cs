namespace WTF.Resource
{
    using WTF.Framework;
    using System;

    public enum VerNoSort
    {
        [Enum("版本号升序")]
        VerNoAsc = 0,
        [Enum("版本号降序")]
        VerNoDesc = 1
    }
}

